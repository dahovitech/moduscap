<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #0d6efd; color: white; padding: 20px; text-align: center; }
        .content { padding: 30px; background: #f8f9fa; }
        .footer { text-align: center; padding: 20px; color: #6c757d; font-size: 12px; }
        .success-icon { font-size: 48px; text-align: center; color: #198754; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>{{ site_name }}</h1>
        </div>
        
        <div class="content">
            <div class="success-icon">✓</div>
            <h2 style="text-align: center;">{{ 'email.contact_confirmation.title'|trans({}, 'default') }}</h2>
            
            <p>{{ 'email.contact_confirmation.greeting'|trans({'%firstName%': contact.firstName}, 'default') }}</p>
            
            <p>{{ 'email.contact_confirmation.received'|trans({'%subject%': contact.subject}, 'default')|raw }}</p>
            
            <p>{{ 'email.contact_confirmation.response_time'|trans({}, 'default') }}</p>
            
            <p>{{ 'email.contact_confirmation.registered'|trans({}, 'default') }}</p>
            <ul>
                <li><strong>{{ 'email.contact_notification.subject'|trans({}, 'default') }}:</strong> {{ contact.subject }}</li>
                <li><strong>{{ 'email.contact_notification.date'|trans({}, 'default') }}:</strong> {{ contact.createdAt|date('d/m/Y H:i') }}</li>
            </ul>
            
            {% if site_phone %}
                <p>{{ 'email.contact_confirmation.immediate_assistance'|trans({'%phone%': site_phone}, 'default')|raw }}</p>
            {% endif %}
            
            <p>Cordialement,<br>L'équipe {{ site_name }}</p>
        </div>
        
        <div class="footer">
            <p>{{ 'email.contact_confirmation.auto_reply'|trans({}, 'default') }}</p>
            <p>&copy; {{ 'now'|date('Y') }} {{ site_name }}. {{ 'email.registration.all_rights_reserved'|trans({}, 'default') }}</p>
            {% if site_address %}
                <p>{{ site_address }}</p>
            {% endif %}
        </div>
    </div>
</body>
</html>
