<!DOCTYPE html>
<html lang="{{ locale|default('fr') }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ 'email.payment_confirmed_title'|trans({'%order_number%': order.orderNumber}) }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f8f9fa;
        }
        .email-container {
            background: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        .header {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: bold;
        }
        .content {
            padding: 30px 20px;
        }
        .success-icon {
            font-size: 48px;
            color: #28a745;
            text-align: center;
            margin: 20px 0;
        }
        .order-info {
            background: #f8f9ff;
            border: 1px solid #e3f2fd;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .order-number {
            font-size: 18px;
            font-weight: bold;
            color: #1976d2;
            margin-bottom: 10px;
        }
        .amount {
            font-size: 24px;
            font-weight: bold;
            color: #28a745;
            margin: 10px 0;
        }
        .payment-confirmed {
            background: #d4edda;
            border: 1px solid #c3e6cb;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .payment-confirmed h3 {
            color: #155724;
            margin: 0 0 15px 0;
        }
        .production-timeline {
            background: #e8f5e8;
            border: 1px solid #c3e6cb;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .production-timeline h3 {
            color: #155724;
            margin: 0 0 15px 0;
        }
        .timeline-stage {
            display: flex;
            align-items: center;
            margin: 10px 0;
            padding: 10px;
            background: white;
            border-radius: 5px;
        }
        .stage-icon {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 15px;
            font-size: 14px;
        }
        .stage-current {
            background: #007bff;
            color: white;
        }
        .stage-upcoming {
            background: #e9ecef;
            color: #6c757d;
        }
        .cta-button {
            display: inline-block;
            background: #007bff;
            color: white;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            margin: 20px 10px;
            text-align: center;
        }
        .footer {
            background: #f8f9fa;
            padding: 20px;
            text-align: center;
            color: #6c757d;
            font-size: 14px;
        }
        .contact-info {
            margin: 20px 0;
            padding: 15px;
            background: #e8f5e8;
            border-radius: 5px;
        }
        .highlight {
            background: #fff3cd;
            padding: 2px 6px;
            border-radius: 3px;
            font-weight: bold;
        }
        .congratulations {
            background: linear-gradient(135deg, #ffeaa7 0%, #fab1a0 100%);
            border-radius: 8px;
            padding: 20px;
            text-align: center;
            margin: 20px 0;
            color: #2d3436;
        }
        ul, ol {
            padding-left: 20px;
        }
        li {
            margin: 8px 0;
        }
        @media (max-width: 600px) {
            body {
                padding: 10px;
            }
            .content {
                padding: 20px 15px;
            }
            .header {
                padding: 20px 15px;
            }
            .cta-button {
                display: block;
                margin: 20px 0;
            }
            .timeline-stage {
                flex-direction: column;
                text-align: center;
            }
            .stage-icon {
                margin-right: 0;
                margin-bottom: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header">
            <h1>💳 {{ 'email.payment_confirmed_header'|trans }}</h1>
            <p>{{ site_name }}</p>
        </div>

        <div class="content">
            <div class="success-icon">🎉</div>
            
            <div class="congratulations">
                <h2>{{ 'email.congratulations_title'|trans }}</h2>
                <p>{{ 'email.congratulations_message'|trans }}</p>
            </div>

            <p>{{ 'email.greeting'|trans({'%name%': client_name }) }}</p>

            <p>{{ 'email.payment_confirmed_intro'|trans({'%order_number%': order_number }) }}</p>

            <div class="order-info">
                <div class="order-number">{{ 'email.order_number'|trans({'%order_number%': order_number}) }}</div>
                <p><strong>{{ 'email.amount_paid'|trans }}:</strong></p>
                <div class="amount">{{ total|number_format(0, ',', ' ') }} €</div>
                <p><strong>{{ 'email.payment_confirmed_on'|trans }}:</strong> {{ paid_at|date('d/m/Y à H:i') }}</p>
            </div>

            <div class="payment-confirmed">
                <h3>✅ {{ 'email.payment_validation'|trans }}</h3>
                <p>{{ 'email.payment_validation_description'|trans }}</p>
                <ul>
                    <li>✅ {{ 'email.amount_verified'|trans }}: {{ total|number_format(0, ',', ' ') }} €</li>
                    <li>✅ {{ 'email.bank_details_confirmed'|trans }}</li>
                    <li>✅ {{ 'email.payment_documents_archived'|trans }}</li>
                    <li>✅ {{ 'email.production_authorized'|trans }}</li>
                </ul>
            </div>

            <div class="production-timeline">
                <h3>🏗️ {{ 'email.production_start'|trans }}</h3>
                <p>{{ 'email.production_start_description'|trans }}</p>
                
                <div class="timeline-stage">
                    <div class="stage-icon stage-current">
                        <i class="fas fa-cog"></i>
                    </div>
                    <div>
                        <strong>{{ 'email.current_phase_production'|trans }}</strong><br>
                        <small>{{ 'email.start_immediately_duration'|trans }}</small>
                    </div>
                </div>
                
                <div class="timeline-stage">
                    <div class="stage-icon stage-upcoming">
                        <i class="fas fa-check"></i>
                    </div>
                    <div>
                        <strong>{{ 'email.quality_control'|trans }}</strong><br>
                        <small>{{ 'email.complete_check_before_shipping'|trans }}</small>
                    </div>
                </div>
                
                <div class="timeline-stage">
                    <div class="stage-icon stage-upcoming">
                        <i class="fas fa-shipping-fast"></i>
                    </div>
                    <div>
                        <strong>{{ 'email.shipping'|trans }}</strong><br>
                        <small>{{ 'email.delivery_installation_on_site'|trans }}</small>
                    </div>
                </div>
            </div>

            <p><strong>{{ 'email.next_steps'|trans }}:</strong></p>
            <ol>
                <li><strong>{{ 'email.immediate_start'|trans }}</strong> - {{ 'email.habitat_fabrication_starts_now'|trans }}</li>
                <li><strong>{{ 'email.regular_updates'|trans }}</strong> - {{ 'email.will_keep_you_informed_progress'|trans }}</li>
                <li><strong>{{ 'email.delivery_schedule'|trans }}</strong> - {{ 'email.detailed_schedule_48h'|trans }}</li>
                <li><strong>{{ 'email.installation_preparation'|trans }}</strong> - {{ 'email.will_guide_site_preparation'|trans }}</li>
            </ol>

            <center>
                <a href="https://{{ app.request.host }}{{ path('app_quote_track', {order_number: order.orderNumber}) }}" class="cta-button">
                    📊 {{ 'email.track_production'|trans }}
                </a>
                <a href="https://{{ app.request.host }}{{ path('app_quote_confirmation', {order_number: order.orderNumber}) }}" class="cta-button">
                    📋 {{ 'email.view_order'|trans }}
                </a>
            </center>

            <div class="contact-info">
                <p><strong>{{ 'email.production_questions'|trans }}</strong></p>
                <p>{{ 'email.team_remains_available'|trans }}</p>
                <p>
                    📧 <a href="mailto:{{ contact_email }}">{{ contact_email }}</a><br>
                    📞 {{ setting.phone|default('+33 1 23 45 67 89') }}
                </p>
                <p><strong>{{ 'email.support_hours'|trans }}:</strong> {{ 'email.support_hours_description'|trans }}</p>
            </div>

            <p>{{ 'email.excited_to_deliver_home'|trans }}</p>

            <p>{{ 'email.best_regards'|trans({'%team%': site_name }) }}</p>
        </div>

        <div class="footer">
            <p>{{ 'email.auto_generated_notice'|trans }}</p>
            <p>© 2024 {{ site_name }} - {{ 'email.all_rights_reserved'|trans }}</p>
        </div>
    </div>
</body>
</html>