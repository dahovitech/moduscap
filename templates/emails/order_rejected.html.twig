<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Information importante - {{ order.orderNumber }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f8f9fa;
        }
        .email-container {
            background: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        .header {
            background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: bold;
        }
        .content {
            padding: 30px 20px;
        }
        .warning-icon {
            font-size: 48px;
            color: #dc3545;
            text-align: center;
            margin: 20px 0;
        }
        .order-info {
            background: #f8f9ff;
            border: 1px solid #e3f2fd;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .order-number {
            font-size: 18px;
            font-weight: bold;
            color: #1976d2;
            margin-bottom: 10px;
        }
        .rejection-reason {
            background: #fff5f5;
            border: 1px solid #fed7d7;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .rejection-reason h3 {
            color: #721c24;
            margin: 0 0 15px 0;
        }
        .alternatives {
            background: #e8f5e8;
            border: 1px solid #c3e6cb;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .alternatives h3 {
            color: #155724;
            margin: 0 0 15px 0;
        }
        .cta-button {
            display: inline-block;
            background: #007bff;
            color: white;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            margin: 20px 10px;
            text-align: center;
        }
        .cta-button-primary {
            background: #28a745;
        }
        .footer {
            background: #f8f9fa;
            padding: 20px;
            text-align: center;
            color: #6c757d;
            font-size: 14px;
        }
        .contact-info {
            margin: 20px 0;
            padding: 15px;
            background: #e8f5e8;
            border-radius: 5px;
        }
        .highlight {
            background: #fff3cd;
            padding: 2px 6px;
            border-radius: 3px;
            font-weight: bold;
        }
        .refund-info {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        ul {
            padding-left: 20px;
        }
        li {
            margin: 8px 0;
        }
        @media (max-width: 600px) {
            body {
                padding: 10px;
            }
            .content {
                padding: 20px 15px;
            }
            .header {
                padding: 20px 15px;
            }
            .cta-button {
                display: block;
                margin: 20px 0;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header">
            <h1>ℹ️ {{ 'email.order_rejected.title'|trans({}, 'default') }}</h1>
            <p>{{ site_name }}</p>
        </div>

        <div class="content">
            <div class="warning-icon">⚠️</div>
            
            <p>{{ 'email.order_rejected.greeting'|trans({'%name%': client_name}, 'default')|raw }}</p>

            <p>{{ 'email.order_rejected.intro'|trans({'%order_number%': order_number}, 'default')|raw }}</p>

            <div class="order-info">
                <div class="order-number">{{ 'email.order_rejected.order_label'|trans({'%order_number%': order_number}, 'default') }}</div>
                <p><strong>{{ 'email.order_rejected.amount'|trans({}, 'default') }}</strong> {{ total|number_format(0, ',', ' ') }} €</p>
                <p><strong>{{ 'email.order_rejected.order_date'|trans({}, 'default') }}</strong> {{ order.createdAt|date('d/m/Y') }}</p>
            </div>

            <div class="rejection-reason">
                <h3>🔍 {{ 'email.order_rejected.reason_title'|trans({}, 'default') }}</h3>
                <p>{{ rejection_reason|nl2br }}</p>
            </div>

            <div class="alternatives">
                <h3>🔄 {{ 'email.order_rejected.alternatives_title'|trans({}, 'default') }}</h3>
                <p>{{ 'email.order_rejected.alternatives_intro'|trans({}, 'default') }}</p>
                <ul>
                    <li>{{ 'email.order_rejected.alternative1'|trans({}, 'default')|raw }}</li>
                    <li>{{ 'email.order_rejected.alternative2'|trans({}, 'default')|raw }}</li>
                    <li>{{ 'email.order_rejected.alternative3'|trans({}, 'default')|raw }}</li>
                    <li>{{ 'email.order_rejected.alternative4'|trans({}, 'default')|raw }}</li>
                </ul>
            </div>

            <div class="refund-info">
                <h3>💰 {{ 'email.order_rejected.refund_title'|trans({}, 'default') }}</h3>
                {% if order.paymentProof %}
                    <p>{{ 'email.order_rejected.refund_with_payment'|trans({}, 'default') }}</p>
                {% else %}
                    <p>{{ 'email.order_rejected.refund_no_payment'|trans({}, 'default') }}</p>
                {% endif %}
            </div>

            <center>
                <a href="mailto:{{ contact_email }}" class="cta-button cta-button-primary">
                    💬 {{ 'email.order_rejected.discuss_alternatives'|trans({}, 'default') }}
                </a>
                <a href="{{ path('app_products_index') }}" class="cta-button">
                    🏠 {{ 'email.order_rejected.see_products'|trans({}, 'default') }}
                </a>
            </center>

            <div class="contact-info">
                <p><strong>{{ 'email.order_rejected.support_available'|trans({}, 'default') }}</strong></p>
                <p>{{ 'email.order_rejected.contact_alternatives'|trans({}, 'default') }}</p>
                <p>
                    📧 <a href="mailto:{{ contact_email }}">{{ contact_email }}</a><br>
                    📞 {{ setting.phone|default('+33 1 23 45 67 89') }}
                </p>
                <p><strong>{{ 'email.order_rejected.opening_hours'|trans({}, 'default') }}</strong> {{ 'email.order_rejected.opening_hours_value'|trans({}, 'default') }}</p>
            </div>

            <p>{{ 'email.order_rejected.thanks'|trans({}, 'default') }}</p>

            <p>{{ 'email.order_rejected.closing'|trans({'%site_name%': site_name}, 'default')|raw }}</p>
        </div>

        <div class="footer">
            <p>{{ 'email.contact_confirmation.auto_reply'|trans({}, 'default') }}</p>
            <p>© 2024 {{ site_name }} - {{ 'email.all_rights_reserved'|trans({}, 'default') }}</p>
        </div>
    </div>
</body>
</html>